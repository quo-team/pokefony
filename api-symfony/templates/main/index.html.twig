{% extends 'base.html.twig' %}

{% block title %}PokeDex !{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Bienvenue sur PokeDex !</h1>

      {% if app.user %}
        <div class="mb-3">
            Bienvenu  {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Se déconnecter</a>
        </div>

    <a href="{{ path('app_training', {'id_user' : app.user.id}) }}"> entrainement </a>
    <br>
    <a href="{{ path('app_trading') }}"> Marché </a>
    <br>
    <a href="{{ path('app_capture') }}"> Chasse </a>
    

    {% else %}



    <a href="{{ path('app_login') }}"> Se connecter </a>
    <br>
    <a href="{{ path('app_register') }}"> S'inscrire </a>
    <br>
    
    {% endif %}
 
</div>
{% endblock %}
